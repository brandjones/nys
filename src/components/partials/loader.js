import React from "react";

const Loader = () => {
  return (
    <div className="ui active inverted dimmer loading">
      <div className="ui large text loader loading">Loading...</div>
    </div>
  );
};

export default Loader;
